// Producción: el frontend (Vercel) llama directamente al backend en Railway.
// Si cambias el dominio del backend, actualiza esta URL y vuelve a desplegar.
export const environment = {
  production: true,
  apiUrl: 'https://contratosappweb-production.up.railway.app',
};
